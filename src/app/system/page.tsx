'use client';
import {useEffect,useState} from 'react';import {BookOpen,CheckCircle2,CircleAlert,FolderTree,Info,RefreshCw,ServerCog} from 'lucide-react';import {AppShell} from '@/components/AppShell';import {TopBar} from '@/components/TopBar';import {api,manualUrl} from '@/lib/api';import type {LogRow,SystemInfo} from '@/types';
export default function SystemPage(){const[info,setInfo]=useState<SystemInfo|null>(null),[version,setVersion]=useState<any>(null),[caps,setCaps]=useState<any[]>([]),[folders,setFolders]=useState<any>(null),[logs,setLogs]=useState<LogRow[]>([]);async function load(){const[i,v,c,f,l]=await Promise.all([api.system(),api.version(),api.capabilities(),api.folderStructure(),api.logs()]);setInfo(i);setVersion(v);setCaps(c);setFolders(f);setLogs(l.items.reverse())}useEffect(()=>{load()},[]);return <AppShell><TopBar info={info} onRefresh={load}/><div className="pageHero"><div><span className="eyebrowText"><ServerCog/> SYSTEM INFORMATION</span><h2>Version, Help & Capability Status</h2><p>Parameter versions, native bridge status, folder model, manual access and clear separation between fully offline functions and hardware-ready adapters.</p></div><div className="pageActions"><a href={manualUrl()} target="_blank"><BookOpen/>Open manual</a><button onClick={load}><RefreshCw/>Refresh</button></div></div><div className="systemGrid"><section className="glassPanel versionCard"><div className="panelHead compact"><div><span className="eyebrowText">VERSION INFO</span><h2>Software / Parameters</h2></div><Info/></div>{version&&<dl className="versionList"><dt>Software</dt><dd>{version.software_version}</dd><dt>Library / bridge</dt><dd>{version.library_version}</dd><dt>ML active</dt><dd>{String(version.machine_learning_active)}</dd><dt>ML model</dt><dd>{version.ml_model_id}</dd><dt>SmartCuvetteMemory</dt><dd>{String(version.smart_cuvette_memory_active)}</dd><dt>Last registration</dt><dd>{version.last_registration}</dd><dt>Lookup table</dt><dd>{version.lookup_table}</dd>{Object.entries(version.parameter_versions||{}).map(([k,v])=><span className="versionPair" key={k}><b>{k}</b><em>{String(v)}</em></span>)}</dl>}</section><section className="glassPanel capabilityCard"><div className="panelHead compact"><div><span className="eyebrowText">IMPLEMENTATION STATUS</span><h2>Manual feature coverage</h2></div><CheckCircle2/></div><div className="capabilityList">{caps.map(c=><div key={c.id}><i className={c.implemented?'ok':'pending'}>{c.implemented?<CheckCircle2/>:<CircleAlert/>}</i><span><b>{c.title}</b><small>{c.notes}</small></span><em>{c.mode}</em></div>)}</div></section><section className="glassPanel folderCard"><div className="panelHead compact"><div><span className="eyebrowText">FOLDER STRUCTURE</span><h2>Inbox / Outbox / Local Settings</h2></div><FolderTree/></div><div className="folderList">{folders?.folders?.map((f:any)=><div key={f.name}><b>{f.name}</b><span>{f.purpose}</span></div>)}</div></section><section className="glassPanel systemLogCard"><div className="panelHead compact"><div><span className="eyebrowText">SYSTEM MESSAGES</span><h2>Timestamped backend log</h2></div></div><div className="messageList tall">{logs.map((l,i)=><div key={i}><time>{new Date(l.time).toLocaleString()}</time><i className={l.level}/><span>{l.message}</span></div>)}</div></section></div></AppShell>}
