'use client';import type {InspectionResult} from '@/types';
export function TrendChart({results}:{results:InspectionResult[]}){const chunks:number[]=[];for(let i=0;i<results.length;i+=Math.max(1,Math.ceil(results.length/16))){const c=results.slice(i,i+Math.max(1,Math.ceil(results.length/16)));chunks.push(c.filter(x=>x.status==='OK').length/Math.max(c.length,1)*100)}const values=chunks.length?chunks:[0],pts=values.map((v,i)=>`${20+i*(560/Math.max(values.length-1,1))},${130-v}`).join(' ');return <div className="trendChart"><svg viewBox="0 0 600 150" preserveAspectRatio="none"><defs><linearGradient id="fill" x1="0" y1="0" x2="0" y2="1"><stop offset="0" stopColor="#38e08b" stopOpacity=".28"/><stop offset="1" stopColor="#38e08b" stopOpacity="0"/></linearGradient></defs><g>{[30,70,110,140].map(y=><line key={y} x1="0" y1={y} x2="600" y2={y} className="chartGrid"/>)}</g><polygon points={`20,145 ${pts} 580,145`} fill="url(#fill)"/><polyline points={pts} fill="none" stroke="#4fe39a" strokeWidth="3" vectorEffect="non-scaling-stroke"/></svg><div className="chartAxis"><span>recent</span><span>Inspection yield trend</span><span>now</span></div></div>}
